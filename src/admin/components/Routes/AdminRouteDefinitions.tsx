import AdminLayout from "admin/AdminLayout";
import { Route, Routes } from "react-router-dom";

type Props = {};

const AdminRouteDefinitions = (props: Props) => {
	return (
		<></>
		
		// <Routes>
		// 	<Route path="admin" element={<AdminLayout />}>
				
		// 	</Route>
		// </Routes>
	);
};

export default AdminRouteDefinitions;